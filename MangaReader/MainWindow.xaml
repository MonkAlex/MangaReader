<Window x:Class="MangaReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local ="clr-namespace:MangaReader"
        xmlns:resx="clr-namespace:MangaReader.Properties"
        Closing="MainWindow_OnClosing"
        Icon="main.ico"
        Title="{x:Static resx:Strings.Title}"  Height="350" Width="525">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:Manga}">
            <StackPanel>
                <Image Source="{Binding Path=Cover}"/>
                <TextBlock Text="{Binding Path=Name}"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Window.TaskbarItemInfo><TaskbarItemInfo x:Name="TaskBar"></TaskbarItemInfo></Window.TaskbarItemInfo>

    <Grid Name="MainGrid">
        <Button Content="{x:Static resx:Strings.Manga_Action_Update}" 
                Margin="0,0,180,5" 
                x:Name="UpdateButton" 
                Click="Update_click" 
                VerticalAlignment="Bottom" 
                HorizontalAlignment="Right" 
                Height="25" 
                Width="75"/>
        <Button Content="{x:Static resx:Strings.Library_Action_Add}" 
                Margin="0,0,100,5" 
                x:Name="AddButton" 
                Click="Add_click" 
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Height="25" 
                Width="75"/>
        <Button Content="{x:Static resx:Strings.Library_Action_Settings}"  
                Margin="0,0,20,5" 
                x:Name="SettingsButton" 
                Click="Settings_click" 
                VerticalAlignment="Bottom" 
                HorizontalAlignment="Right" 
                Height="25" 
                Width="75"/>
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                 MouseDoubleClick="Mangas_clicked"
                 MouseRightButtonUp="FormLibrary_OnMouseRightButtonUp"
                 x:Name="FormLibrary" 
                 Margin="10,10,10.4,38.4">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"
                               ItemWidth="140"
                               x:Name="ListWrapPanel"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <StatusBar x:Name="StatusBar"
                   Margin="18,0,260.4,5.4" 
                   Height="25" 
                   VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="TextBlock" TextAlignment="Center"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
