<Window x:Class="MangaReader.Services.VersionHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:MangaReader.Properties"
        Title="{x:Static properties:Strings.Update_Title}" Height="400" Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="../Icons/main.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TextBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap" Text="TextBox" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        <Button Content="Ok" Grid.Row="1" Grid.Column="1" Click="Ok_Click" IsDefault="True"/>
        <StatusBar  Grid.Row="1" Grid.Column="0">
            <StatusBarItem HorizontalContentAlignment="Center">
                <TextBlock x:Name="Label"/>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
